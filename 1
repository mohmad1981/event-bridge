pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                sh 'python3 -m pip install requests'
            }
        }

        stage('Run script') {
            steps {
                sh 'python3 br3.py'
            }
        }
    }
}
